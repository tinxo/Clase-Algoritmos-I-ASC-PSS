/**
 * Ejemplo de uso de funciones 
 */

// Se define la función suma que va a tener como parámetros dos valores a y b
function suma(a, b) {
    // la función va a sumar dos valores y devuelve el resultado
    return a + b;
}

let numA = 5;
let numB = 10;

// se llama o invoca a la función para ejecutarla pasando las variables numA y numB como argumentos
let resultado = suma(numA, numB); // el resultado se guarda en una variable
console.log(resultado);